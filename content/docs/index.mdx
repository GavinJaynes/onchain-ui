---
title: Introduction
description: Open source shadcn registry of web3 UI components built with wagmi + viem.
---

## What is onchain-ui?

onchain-ui is an open source [shadcn registry](https://ui.shadcn.com/docs/registry) of web3 UI components.
Components are built with [wagmi](https://wagmi.sh) and [viem](https://viem.sh), and styled with Tailwind CSS.

You own the code. Copy and paste, or install directly:

```bash
npx shadcn add https://onchain-ui.dev/r/address-display
```

## Philosophy

- **Copy-paste first** - components live in your codebase, not in node_modules
- **wagmi + viem** - typed, composable, the ecosystem standard
- **shadcn primitives** - built on the same components you already have
- **Chain-agnostic** - works across EVM chains

## Getting Started

Install the [shadcn CLI](https://ui.shadcn.com/docs/cli) if you haven't already, then add any component:

```bash
npx shadcn add https://onchain-ui.dev/r/address-display
npx shadcn add https://onchain-ui.dev/r/token-logo
npx shadcn add https://onchain-ui.dev/r/asset-row
```
